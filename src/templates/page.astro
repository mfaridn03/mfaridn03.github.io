---
import Base from "@layouts/Base.astro";
---

<Base>
  {
    /* Add your page content here.
     *
     * Wrapping content in <article> gives it Pico's default block styling
     * (padding, background, border). */
  }
</Base>
